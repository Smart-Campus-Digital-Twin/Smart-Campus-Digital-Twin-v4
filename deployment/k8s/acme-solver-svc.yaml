apiVersion: v1
kind: Service
metadata:
  name: cm-acme-http-solver
  namespace: campus
spec:
  selector:
    acme.cert-manager.io/http01-solver: "true"
  ports:
    - port: 8089
      targetPort: 8089
